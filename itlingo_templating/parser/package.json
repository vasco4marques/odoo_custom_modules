{
  "name": "itlingo-dsl-parser",
  "version": "1.0.0",
  "private": true,
  "description": "Runtime DSL grammar and source to JSON AST CLI.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/parser.mjs --bundle --platform=node --format=esm --external:langium --outfile=dist/parser.mjs"
  },
  "dependencies": {
    "langium": "4.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
