<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Workspace List -->
    <template id="portal_my_workspaces" name="My Workspaces">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Workspaces</t>
            </t>
            <div t-if="ws_list_message == 'ws_status_updated'" class="alert alert-success mt-2" role="alert">
                Workspace status updated.
            </div>
            <div t-if="ws_list_message == 'ws_deleted'" class="alert alert-success mt-2" role="alert">
                The workspace was deleted.
            </div>
            <div t-if="ws_list_message == 'ws_deleted_self_leave'" class="alert alert-success mt-2" role="alert">
                You were the only member, so the workspace was deleted when you left.
            </div>
            <div t-if="ws_list_message == 'left_workspace'" class="alert alert-success mt-2" role="alert">
                You left the workspace.
            </div>
            <div t-if="ws_list_error == 'ws_bad_status'" class="alert alert-danger mt-2" role="alert">
                Invalid status action.
            </div>
            <div t-if="ws_list_error == 'ws_status_failed'" class="alert alert-danger mt-2" role="alert">
                Could not change status for this workspace (invalid state or permission).
            </div>
            <div t-if="ws_list_error == 'ws_delete_failed'" class="alert alert-danger mt-2" role="alert">
                Could not delete this workspace. It may have linked records blocking removal.
            </div>
            <div t-if="can_create_workspace" class="d-flex flex-wrap align-items-center gap-2 mb-3">
                <a href="/my/workspaces/new" class="btn btn-primary">
                    <i class="fa fa-plus me-1"/>New workspace
                </a>
            </div>
            <div class="card itlingo-portal-filter-card shadow-sm border-0 rounded-3 mb-4">
                <div class="card-body">
                    <p class="text-muted small text-uppercase fw-semibold mb-3">Workspace filters</p>
                    <!-- One filter per column, in column order:
                         Workspace (name) | Organization | Your Role | Public | State -->
                    <form method="get" action="/my/workspaces" class="row g-3 align-items-end">
                        <div class="col-md-4 col-lg-3">
                            <label class="form-label small text-muted mb-1" for="ws_filter_name">Name</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0"><i class="fa fa-search text-muted"/></span>
                                <input type="search" class="form-control border-start-0 bg-light" id="ws_filter_name"
                                       name="name" t-att-value="filters.get('name') or ''" placeholder="Name"/>
                            </div>
                        </div>
                        <div class="col-md-4 col-lg-2">
                            <t t-call="itlingo_website_portal.portal_filter_multiselect">
                                <t t-set="ms_id" t-value="'ws_filter_org'"/>
                                <t t-set="ms_label">Organization</t>
                                <t t-set="ms_options" t-value="[
                                    {'name': 'org_id', 'value': org.id, 'label': org.name,
                                     'checked': org.id in (filters.get('org_id') or [])}
                                    for org in ws_org_options]"/>
                            </t>
                        </div>
                        <div class="col-md-4 col-lg-2">
                            <t t-call="itlingo_website_portal.portal_filter_multiselect">
                                <t t-set="ms_id" t-value="'ws_filter_roles'"/>
                                <t t-set="ms_label">Your Role</t>
                                <t t-set="ms_options" t-value="[
                                    {'name': 'role_ws_manager', 'value': '1', 'label': 'Workspace Manager',
                                     'checked': filters.get('role_ws_manager')},
                                    {'name': 'role_doc_manager', 'value': '1', 'label': 'Document Manager',
                                     'checked': filters.get('role_doc_manager')},
                                    {'name': 'role_ws_member', 'value': '1', 'label': 'Workspace Member',
                                     'checked': filters.get('role_ws_member')}]"/>
                            </t>
                        </div>
                        <div class="col-md-4 col-lg-2">
                            <t t-call="itlingo_website_portal.portal_filter_multiselect">
                                <t t-set="ms_id" t-value="'ws_filter_visibility'"/>
                                <t t-set="ms_label">Public</t>
                                <t t-set="ms_options" t-value="[
                                    {'name': 'public_ws', 'value': '1', 'label': 'Yes',
                                     'checked': filters.get('public_ws')},
                                    {'name': 'private_ws', 'value': '1', 'label': 'No',
                                     'checked': filters.get('private_ws')}]"/>
                            </t>
                        </div>
                        <div class="col-md-4 col-lg-2">
                            <t t-call="itlingo_website_portal.portal_filter_multiselect">
                                <t t-set="ms_id" t-value="'ws_filter_status'"/>
                                <t t-set="ms_label">State</t>
                                <t t-set="ms_options" t-value="[
                                    {'name': 'status', 'value': opt[0], 'label': opt[1],
                                     'checked': opt[0] in (filters.get('status') or [])}
                                    for opt in ws_status_selection]"/>
                            </t>
                        </div>
                        <div class="col-12 d-flex flex-wrap gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="fa fa-search me-1"/>Filter
                            </button>
                            <a href="/my/workspaces" class="btn btn-outline-secondary">Clear filters</a>
                        </div>
                    </form>
                </div>
            </div>
            <t t-if="not projects">
                <div class="alert alert-light border" role="alert">
                    No workspaces match your filters, or you are not a member of any workspace yet.
                </div>
            </t>
            <div t-if="projects" class="card border-0 shadow-sm rounded-3">
                <div class="card-header bg-white border-0 p-3">
                    <h5 class="fw-semibold mb-0">Workspaces (<t t-out="ws_count"/>)</h5>
                </div>
                <div class="card-body p-0 table-responsive">
                    <table class="table table-hover mb-0 align-middle">
                <thead class="table-light">
                    <tr>
                        <th>
                            <t t-call="itlingo_website_portal.portal_sort_th">
                                <t t-set="sort_key" t-value="'name'"/>
                                <t t-set="sort_label">Workspace</t>
                            </t>
                        </th>
                        <th>
                            <t t-call="itlingo_website_portal.portal_sort_th">
                                <t t-set="sort_key" t-value="'organization'"/>
                                <t t-set="sort_label">Organization</t>
                            </t>
                        </th>
                        <th>Your Role</th>
                        <th>
                            <t t-call="itlingo_website_portal.portal_sort_th">
                                <t t-set="sort_key" t-value="'public'"/>
                                <t t-set="sort_label">Public</t>
                            </t>
                        </th>
                        <th>
                            <t t-call="itlingo_website_portal.portal_sort_th">
                                <t t-set="sort_key" t-value="'status'"/>
                                <t t-set="sort_label">State</t>
                            </t>
                        </th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="projects" t-as="proj">
                        <t t-set="role" t-value="role_map.get(proj.id)"/>
                        <td>
                            <a t-attf-href="/my/workspaces/#{proj.id}">
                                <t t-out="proj.name"/>
                            </a>
                        </td>
                        <td>
                            <t t-if="proj.organization_id">
                                <a t-attf-href="/my/organizations/#{proj.organization_id.id}">
                                    <t t-out="proj.organization_id.name"/>
                                </a>
                            </t>
                            <t t-else="">—</t>
                        </td>
                        <td>
                            <!-- text-wrap lets the pill break on narrow screens so the
                                 table fits a 768px viewport without clipping -->
                            <span t-if="role" class="badge bg-info text-dark text-wrap">
                                <t t-out="dict(role._fields['role'].selection).get(role.role)"/>
                            </span>
                        </td>
                        <td>
                            <span t-if="proj.is_public_workspace" class="badge bg-success">Yes</span>
                            <span t-else="" class="badge bg-light text-dark border">No</span>
                        </td>
                        <td>
                            <t t-call="itlingo_website_portal.portal_ws_status_badge">
                                <t t-set="ws_status_project" t-value="proj"/>
                            </t>
                        </td>
                        <td class="text-end">
                            <a t-attf-href="/my/workspaces/#{proj.id}/documents"
                               class="btn btn-sm btn-link text-primary p-1"
                               title="Documents" aria-label="Documents">
                                <i class="fa fa-file-text-o fa-lg"/>
                            </a>
                            <a t-attf-href="/my/workspaces/#{proj.id}/users"
                               class="btn btn-sm btn-link text-primary p-1"
                               title="Users and roles" aria-label="Users and roles">
                                <i class="fa fa-users fa-lg"/>
                            </a>
                            <t t-if="role and role.role == 'ws_manager'">
                                <a t-attf-href="/my/workspaces/#{proj.id}/settings"
                                   class="btn btn-sm btn-link text-primary p-1"
                                   title="Workspace settings" aria-label="Workspace settings">
                                    <i class="fa fa-cog fa-lg"/>
                                </a>
                                <form t-attf-action="/my/workspaces/#{proj.id}/delete" method="post" class="d-inline">
                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                    <button type="submit" class="btn btn-sm btn-link text-danger p-1"
                                            title="Delete workspace" aria-label="Delete workspace"
                                            onclick="return confirm('Delete this workspace? This cannot be undone.');">
                                        <i class="fa fa-trash fa-lg"/>
                                    </button>
                                </form>
                            </t>
                        </td>
                    </tr>
                </tbody>
                    </table>
                </div>
            </div>
            <t t-call="itlingo_website_portal.portal_list_pager"/>
        </t>
    </template>

    <!-- Public workspace directory (anonymous and portal users) -->
    <template id="portal_public_workspaces" name="Public workspaces">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Public workspaces</t>
            </t>
            <p class="text-muted small mb-3">
                These workspaces are shared publicly.
            </p>
            <div class="card itlingo-portal-filter-card shadow-sm border-0 rounded-3 mb-4">
                <div class="card-body">
                    <!-- One filter per column, in column order:
                         Workspace (name) | Organization | State -->
                    <form method="get" action="/public-workspaces" class="row g-3 align-items-end">
                        <div class="col-md-4 col-lg-3">
                            <label class="form-label small text-muted mb-1" for="pub_ws_name">Name</label>
                            <input type="search" class="form-control" id="pub_ws_name" name="name"
                                   t-att-value="filters.get('name') or ''" placeholder="Search by name"/>
                        </div>
                        <div class="col-md-4 col-lg-3">
                            <t t-call="itlingo_website_portal.portal_filter_multiselect">
                                <t t-set="ms_id" t-value="'pub_ws_filter_org'"/>
                                <t t-set="ms_label">Organization</t>
                                <t t-set="ms_options" t-value="[
                                    {'name': 'org_id', 'value': org.id, 'label': org.name,
                                     'checked': org.id in (filters.get('org_id') or [])}
                                    for org in ws_org_options]"/>
                            </t>
                        </div>
                        <div class="col-md-4 col-lg-3">
                            <t t-call="itlingo_website_portal.portal_filter_multiselect">
                                <t t-set="ms_id" t-value="'pub_ws_filter_status'"/>
                                <t t-set="ms_label">State</t>
                                <t t-set="ms_options" t-value="[
                                    {'name': 'status', 'value': opt[0], 'label': opt[1],
                                     'checked': opt[0] in (filters.get('status') or [])}
                                    for opt in ws_status_selection]"/>
                            </t>
                        </div>
                        <div class="col-12 d-flex flex-wrap gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="fa fa-search me-1"/>Filter
                            </button>
                            <a href="/public-workspaces" class="btn btn-outline-secondary">Clear filters</a>
                        </div>
                    </form>
                </div>
            </div>
            <t t-if="not projects">
                <div class="alert alert-light border" role="alert">
                    No public workspaces match your search.
                </div>
            </t>
            <t t-if="projects" t-call="portal.portal_table">
                <thead>
                    <tr>
                        <th>
                            <t t-call="itlingo_website_portal.portal_sort_th">
                                <t t-set="sort_key" t-value="'name'"/>
                                <t t-set="sort_label">Workspace</t>
                            </t>
                        </th>
                        <th>
                            <t t-call="itlingo_website_portal.portal_sort_th">
                                <t t-set="sort_key" t-value="'organization'"/>
                                <t t-set="sort_label">Organization</t>
                            </t>
                        </th>
                        <th>
                            <t t-call="itlingo_website_portal.portal_sort_th">
                                <t t-set="sort_key" t-value="'create_date'"/>
                                <t t-set="sort_label">Create Date</t>
                            </t>
                        </th>
                        <th>
                            <t t-call="itlingo_website_portal.portal_sort_th">
                                <t t-set="sort_key" t-value="'status'"/>
                                <t t-set="sort_label">State</t>
                            </t>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="public_workspace_rows" t-as="pw_row">
                        <t t-set="proj" t-value="pw_row[0]"/>
                        <t t-set="pw_url" t-value="pw_row[1]"/>
                        <t t-set="bs" t-value="proj.business_status or 'not_started'"/>
                        <td>
                            <a t-att-href="pw_url">
                                <t t-out="proj.name"/>
                            </a>
                        </td>
                        <td>
                            <t t-if="proj.organization_id" t-out="proj.organization_id.name"/>
                            <t t-else="">—</t>
                        </td>
                        <td>
                            <t t-call="itlingo_website_portal.portal_date">
                                <t t-set="date_value" t-value="proj.create_date"/>
                            </t>
                        </td>
                        <td>
                            <t t-call="itlingo_website_portal.portal_ws_status_badge">
                                <t t-set="ws_status_project" t-value="proj"/>
                            </t>
                        </td>
                    </tr>
                </tbody>
            </t>
            <t t-call="itlingo_website_portal.portal_list_pager"/>
        </t>
    </template>

    <!-- Public read-only document detail (no chatter) -->
    <template id="portal_public_workspace_document" name="Public workspace document">
        <t t-call="portal.portal_layout">
            <t t-set="hide_project_breadcrumbs" t-value="True"/>
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title" t-value="document.name"/>
            </t>
            <div class="card border-0 shadow-sm rounded-3">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h3 class="mb-0"><t t-out="document.name"/></h3>
                    <span t-attf-class="badge rounded-pill
                        #{'text-bg-secondary' if document.status == 'draft'
                          else 'text-bg-warning' if document.status == 'in_review'
                          else 'text-bg-success' if document.status == 'approved'
                          else 'text-bg-primary' if document.status == 'published'
                          else 'text-bg-dark'}">
                        <t t-out="dict(document._fields['status'].selection).get(document.status)"/>
                    </span>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <dl>
                                <dt>Workspace</dt>
                                <dd><t t-out="document.project_id.name or 'N/A'"/></dd>
                                <dt>Organization</dt>
                                <dd><t t-out="document.organization_id.name or 'N/A'"/></dd>
                                <dt>Document Type</dt>
                                <dd><t t-out="document.document_type_id.name or 'N/A'"/></dd>
                                <dt>DSL</dt>
                                <dd>
                                    <t t-if="document.dsl_id" t-out="document.dsl_id.acronym"/>
                                    <t t-else="">—</t>
                                </dd>
                            </dl>
                        </div>
                        <div class="col-md-6">
                            <dl>
                                <dt>Language</dt>
                                <dd>
                                    <t t-if="document.language" t-out="dict(document._fields['language'].selection).get(document.language)"/>
                                    <t t-else="">—</t>
                                </dd>
                                <dt>Format</dt>
                                <dd>
                                    <t t-if="document.document_format" t-out="dict(document._fields['document_format'].selection).get(document.document_format)"/>
                                    <t t-else="">—</t>
                                </dd>
                                <dt>Version</dt>
                                <dd><t t-out="document.version"/></dd>
                                <dt>Created</dt>
                                <dd>
                                    <t t-call="itlingo_website_portal.portal_date">
                                        <t t-set="date_value" t-value="document.creation_date"/>
                                    </t>
                                </dd>
                                <dt>Last Modified</dt>
                                <dd>
                                    <t t-call="itlingo_website_portal.portal_date">
                                        <t t-set="date_value" t-value="document.write_date"/>
                                    </t>
                                </dd>
                            </dl>
                        </div>
                    </div>
                    <div t-if="document.document_file" class="mt-3">
                        <a t-attf-href="/public-workspaces/#{project.id}/documents/#{document.id}/download"
                           class="btn btn-primary">
                            <i class="fa fa-download"/> Download file
                        </a>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <!-- Workspace hub shell: sidebar + main (slot in child templates) -->
    <template id="portal_workspace_hub_shell" name="Workspace hub shell">
        <t t-call="portal.portal_layout">
            <t t-set="hide_project_breadcrumbs" t-value="True"/>
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title" t-value="project.name"/>
            </t>
            <div class="itlingo-portal-page--tint rounded-3 p-3 p-lg-4">
                <div class="row g-4 itlingo-org-hub">
                    <aside class="col-lg-3">
                        <nav class="itlingo-org-hub__sidebar nav flex-column rounded-3 shadow-sm bg-white p-2"
                             aria-label="Workspace sections">
                            <a t-att-href="ws_hub_prefix"
                               t-attf-class="nav-link itlingo-org-hub__nav-link rounded-2 #{workspace_hub_page == 'home' and 'active' or ''}">
                                <i class="fa fa-home fa-fw me-2" aria-hidden="true"/>Home
                            </a>
                            <a t-attf-href="#{ws_hub_prefix}/documents"
                               t-attf-class="nav-link itlingo-org-hub__nav-link rounded-2 #{workspace_hub_page == 'documentation' and 'active' or ''}">
                                <i class="fa fa-file-text-o fa-fw me-2" aria-hidden="true"/>Documents
                            </a>
                            <a t-attf-href="#{ws_hub_prefix}/specifications"
                               t-attf-class="nav-link itlingo-org-hub__nav-link rounded-2 #{workspace_hub_page == 'specifications' and 'active' or ''}">
                                <i class="fa fa-code fa-fw me-2" aria-hidden="true"/>Specifications
                            </a>
                            <a t-if="not workspace_hub_public" t-attf-href="#{ws_hub_prefix}/users"
                               t-attf-class="nav-link itlingo-org-hub__nav-link rounded-2 #{workspace_hub_page == 'users' and 'active' or ''}">
                                <i class="fa fa-users fa-fw me-2" aria-hidden="true"/>Users
                            </a>
                            <a t-if="not workspace_hub_public and user_role and user_role.role == 'ws_manager'"
                               t-attf-href="#{ws_hub_prefix}/settings"
                               t-attf-class="nav-link itlingo-org-hub__nav-link rounded-2 #{workspace_hub_page == 'settings' and 'active' or ''}">
                                <i class="fa fa-cog fa-fw me-2" aria-hidden="true"/>Settings
                            </a>
                        </nav>
                    </aside>
                    <div class="col-lg-9 itlingo-org-hub__main">
                        <h1 class="itlingo-org-hub__title mb-4">
                            <t t-if="project.organization_id and not workspace_hub_public">
                                <a t-attf-href="/my/organizations/#{project.organization_id.id}" class="text-reset text-decoration-none">
                                    <t t-out="project.organization_id.name"/>
                                </a>:
                            </t>
                            <t t-if="project.organization_id and workspace_hub_public">
                                <span class="text-muted"><t t-out="project.organization_id.name"/>:</span>
                            </t>
                            <t t-out="project.name"/>
                        </h1>
                        <t t-out="0"/>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <!-- Workspace Detail / Home dashboard -->
    <template id="portal_workspace_detail" name="Workspace Detail">
        <t t-call="itlingo_website_portal.portal_workspace_hub_shell">
            <div t-if="ws_message == 'ws_status_updated'" class="alert alert-success" role="alert">
                Workspace status updated.
            </div>
            <div t-if="ws_error == 'ws_bad_status'" class="alert alert-danger" role="alert">
                Invalid status action.
            </div>
            <div t-if="ws_error == 'ws_status_failed'" class="alert alert-danger" role="alert">
                Could not change status for this workspace (invalid state or permission).
            </div>
            <!-- Description: full-width HTML block, no label (D.2 a) -->
            <div t-if="project.description" class="card border-0 shadow-sm rounded-3 mb-4">
                <div class="card-body">
                    <div t-field="project.description"/>
                </div>
            </div>

            <!-- Grouped indicator cards, same design as the org dashboard (D.2 c) -->
            <t t-if="stats_cards">
                <t t-call="itlingo_website_portal.portal_stats_cards">
                    <t t-set="stats_col_class" t-value="'col-12 col-md-6'"/>
                </t>
            </t>

            <!-- Compact metadata (D.2 b) -->
            <div class="card border-0 shadow-sm rounded-3 mt-4">
                <div class="card-body">
                    <h2 class="h6 text-muted mb-3">Details</h2>
                    <dl class="row mb-0 small">
                        <dt class="col-sm-4 text-muted">Creator</dt>
                        <dd class="col-sm-8">
                            <t t-if="project.creator_id" t-out="project.creator_id.name"/>
                            <t t-else="">—</t>
                        </dd>
                        <dt class="col-sm-4 text-muted">Organization</dt>
                        <dd class="col-sm-8">
                            <t t-if="project.organization_id and not workspace_hub_public">
                                <a t-attf-href="/my/organizations/#{project.organization_id.id}">
                                    <t t-out="project.organization_id.name"/>
                                </a>
                            </t>
                            <t t-if="project.organization_id and workspace_hub_public">
                                <t t-out="project.organization_id.name"/>
                            </t>
                            <t t-if="not project.organization_id">—</t>
                        </dd>
                        <dt t-if="user_role" class="col-sm-4 text-muted">Your role</dt>
                        <dd t-if="user_role" class="col-sm-8">
                            <span class="badge bg-info text-dark">
                                <t t-out="dict(user_role._fields['role'].selection).get(user_role.role)"/>
                            </span>
                        </dd>
                        <dt class="col-sm-4 text-muted">State</dt>
                        <dd class="col-sm-8">
                            <div class="d-flex flex-wrap align-items-center gap-2">
                                <t t-call="itlingo_website_portal.portal_ws_status_badge">
                                    <t t-set="ws_status_project" t-value="project"/>
                                </t>
                                <t t-if="user_role and user_role.role == 'ws_manager'">
                                    <t t-set="bs" t-value="project.business_status or 'not_started'"/>
                                    <form t-attf-action="/my/workspaces/#{project.id}/business_status" method="post" class="d-inline">
                                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                        <input type="hidden" name="transition" value="executing"/>
                                        <button type="submit" class="btn btn-sm btn-link text-primary p-1"
                                                title="Set executing" aria-label="Set executing"
                                                t-att-disabled="bs == 'executing' or bs == 'concluded'">
                                            <i class="fa fa-play-circle fa-lg"/>
                                        </button>
                                    </form>
                                    <form t-attf-action="/my/workspaces/#{project.id}/business_status" method="post" class="d-inline">
                                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                        <input type="hidden" name="transition" value="canceled"/>
                                        <button type="submit" class="btn btn-sm btn-link text-primary p-1"
                                                title="Set canceled" aria-label="Set canceled"
                                                t-att-disabled="bs == 'canceled'">
                                            <i class="fa fa-times-circle fa-lg"/>
                                        </button>
                                    </form>
                                    <form t-attf-action="/my/workspaces/#{project.id}/business_status" method="post" class="d-inline">
                                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                        <input type="hidden" name="transition" value="concluded"/>
                                        <button type="submit" class="btn btn-sm btn-link text-primary p-1"
                                                title="Set concluded" aria-label="Set concluded"
                                                t-att-disabled="bs == 'concluded' or bs == 'canceled'">
                                            <i class="fa fa-check-circle fa-lg"/>
                                        </button>
                                    </form>
                                </t>
                            </div>
                        </dd>
                        <dt class="col-sm-4 text-muted">Creation date</dt>
                        <dd class="col-sm-8">
                            <t t-call="itlingo_website_portal.portal_date">
                                <t t-set="date_value" t-value="project.create_date"/>
                            </t>
                        </dd>
                        <dt class="col-sm-4 text-muted">Planned start</dt>
                        <dd class="col-sm-8">
                            <t t-call="itlingo_website_portal.portal_date">
                                <t t-set="date_value" t-value="project.planned_start"/>
                            </t>
                        </dd>
                        <dt class="col-sm-4 text-muted">Planned end</dt>
                        <dd class="col-sm-8">
                            <t t-call="itlingo_website_portal.portal_date">
                                <t t-set="date_value" t-value="project.planned_end"/>
                            </t>
                        </dd>
                        <dt class="col-sm-4 text-muted">Actual start</dt>
                        <dd class="col-sm-8">
                            <t t-call="itlingo_website_portal.portal_date">
                                <t t-set="date_value" t-value="project.actual_start"/>
                            </t>
                        </dd>
                        <dt class="col-sm-4 text-muted">Actual end</dt>
                        <dd class="col-sm-8">
                            <t t-call="itlingo_website_portal.portal_date">
                                <t t-set="date_value" t-value="project.actual_end"/>
                            </t>
                        </dd>
                    </dl>
                </div>
            </div>
        </t>
    </template>

    <!-- Workspace users (members + invite) -->
    <template id="portal_workspace_users" name="Workspace Users">
        <t t-call="itlingo_website_portal.portal_workspace_hub_shell">
            <div t-if="ws_message == 'invite_sent'" class="alert alert-success" role="alert">
                Invitation sent successfully.
            </div>
            <div t-if="ws_message == 'invite_resent'" class="alert alert-success" role="alert">
                Invitation resent.
            </div>
            <div t-if="ws_message == 'role_updated'" class="alert alert-success" role="alert">
                Role updated.
            </div>
            <div t-if="ws_message == 'member_removed'" class="alert alert-success" role="alert">
                Member removed from the workspace.
            </div>
            <div t-if="ws_error == 'invite_email_required'" class="alert alert-danger" role="alert">
                Enter an email address to invite.
            </div>
            <div t-if="ws_error == 'invite_already_member'" class="alert alert-danger" role="alert">
                This user already has a role in the workspace.
            </div>
            <div t-if="ws_error == 'invite_bad_role'" class="alert alert-danger" role="alert">
                Invalid role for invitation.
            </div>
            <div t-if="ws_error == 'last_manager'" class="alert alert-danger" role="alert">
                The workspace must keep at least one workspace manager.
            </div>
            <div t-if="ws_error == 'role_invalid'" class="alert alert-danger" role="alert">
                Invalid role.
            </div>
            <div t-if="ws_error in ('role_not_accepted', 'remove_not_accepted')" class="alert alert-danger" role="alert">
                That membership is not active.
            </div>

            <div t-if="can_manage_ws_users" class="d-flex flex-wrap gap-2 mb-3">
                <button class="btn btn-primary" type="button" data-bs-toggle="collapse"
                        data-bs-target="#ws_invite_panel" aria-expanded="false"
                        aria-controls="ws_invite_panel">
                    <i class="fa fa-user-plus me-1"/>Invite member
                </button>
            </div>
            <div t-if="can_manage_ws_users" class="collapse mb-4" id="ws_invite_panel">
                <div class="card border-0 shadow-sm rounded-3">
                    <div class="card-header bg-white border-0 p-3">
                        <h5 class="fw-semibold mb-0">Invite member</h5>
                    </div>
                    <div class="card-body">
                        <form t-attf-action="/my/workspaces/#{project.id}/users/invite" method="post" class="row g-3 align-items-end">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <input type="hidden" name="redirect_users" value="1"/>
                            <div class="col-md-5">
                                <label class="form-label" for="ws_invite_email">Email (login)</label>
                                <input type="email" class="form-control" id="ws_invite_email" name="email" required="" placeholder="user@example.com"/>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label" for="ws_invite_role">Role</label>
                                <select class="form-select" id="ws_invite_role" name="role">
                                    <option value="ws_member">Workspace Member</option>
                                    <option value="doc_manager">Document Manager</option>
                                    <option value="ws_manager">Workspace Manager</option>
                                </select>
                            </div>
                            <div class="col-md-3">
                                <button type="submit" class="btn btn-primary w-100">Send invitation</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
            <div class="card itlingo-portal-filter-card shadow-sm border-0 rounded-3 mb-4">
                <div class="card-body">
                    <p class="text-muted small text-uppercase fw-semibold mb-3">Member filters</p>
                    <form method="get" t-att-action="users_filter_url">
                        <div class="row g-3">
                            <div class="col-12 col-md-6">
                                <label class="form-label small text-muted mb-1" for="ws_user_filter_name">Name or email</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light border-end-0"><i class="fa fa-search text-muted"/></span>
                                    <input type="search" class="form-control border-start-0 bg-light" id="ws_user_filter_name"
                                           name="name" t-att-value="users_filters.get('name') or ''" placeholder="Name or email"/>
                                </div>
                            </div>
                            <div class="col-12 col-md-6">
                                <t t-call="itlingo_website_portal.portal_filter_multiselect">
                                    <t t-set="ms_id" t-value="'ws_user_filter_role'"/>
                                    <t t-set="ms_label">Role</t>
                                    <t t-set="ms_options" t-value="[
                                        {'name': 'role_ws_manager', 'value': '1', 'label': 'Workspace Manager',
                                         'checked': users_filters.get('role_ws_manager')},
                                        {'name': 'role_doc_manager', 'value': '1', 'label': 'Document Manager',
                                         'checked': users_filters.get('role_doc_manager')},
                                        {'name': 'role_ws_member', 'value': '1', 'label': 'Workspace Member',
                                         'checked': users_filters.get('role_ws_member')}]"/>
                                </t>
                            </div>
                            <div class="col-12 d-flex flex-wrap gap-2 pt-1">
                                <button type="submit" class="btn btn-primary">
                                    <i class="fa fa-search me-1"/>Filter
                                </button>
                                <a t-att-href="users_filter_url" class="btn btn-outline-secondary">Clear filters</a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
            <div class="card border-0 shadow-sm rounded-3">
                <div class="card-header bg-white border-0 p-3">
                    <h5 class="fw-semibold mb-0">Members (<t t-out="member_count"/>)</h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover mb-0 align-middle">
                            <thead class="table-light">
                                <tr>
                                    <th>
                                        <t t-call="itlingo_website_portal.portal_sort_th">
                                            <t t-set="sort_key" t-value="'user'"/>
                                            <t t-set="sort_label">User</t>
                                        </t>
                                    </th>
                                    <th>Email</th>
                                    <th>
                                        <t t-call="itlingo_website_portal.portal_sort_th">
                                            <t t-set="sort_key" t-value="'role'"/>
                                            <t t-set="sort_label">Role</t>
                                        </t>
                                    </th>
                                    <th>
                                        <t t-call="itlingo_website_portal.portal_sort_th">
                                            <t t-set="sort_key" t-value="'member_since'"/>
                                            <t t-set="sort_label">Member Since</t>
                                        </t>
                                    </th>
                                    <th class="text-end" style="width: 8rem;">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr t-foreach="members" t-as="member">
                                    <t t-set="editing" t-value="can_manage_ws_users and edit_role_id == member.id"/>
                                    <td><t t-out="member.user_id.name"/></td>
                                    <td class="text-muted"><t t-out="member.user_id.login"/></td>
                                    <td>
                                        <span t-if="not editing" class="badge bg-info text-dark">
                                            <t t-out="dict(member._fields['role'].selection).get(member.role)"/>
                                        </span>
                                        <form t-if="editing"
                                              t-attf-action="/my/workspaces/#{project.id}/users/#{member.id}/role"
                                              method="post" class="d-flex flex-wrap gap-2 align-items-center">
                                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                            <input type="hidden" name="redirect_users" value="1"/>
                                            <select class="form-select form-select-sm" style="min-width: 10rem;" name="role">
                                                <t t-foreach="member._fields['role'].selection" t-as="sel">
                                                    <option t-att-value="sel[0]"
                                                            t-att-selected="member.role == sel[0]">
                                                        <t t-out="sel[1]"/>
                                                    </option>
                                                </t>
                                            </select>
                                            <button type="submit" class="btn btn-sm btn-primary">Save</button>
                                            <a t-att-href="users_filter_url" class="btn btn-sm btn-outline-secondary">Cancel</a>
                                        </form>
                                    </td>
                                    <td>
                                        <t t-call="itlingo_website_portal.portal_date">
                                            <t t-set="date_value" t-value="member.response_date or member.create_date"/>
                                        </t>
                                    </td>
                                    <td class="text-end">
                                        <div class="d-inline-flex justify-content-end">
                                            <span class="d-inline-block text-center" style="width: 2.25rem;">
                                                <a t-if="can_manage_ws_users and not editing"
                                                   t-attf-href="#{users_filter_url}?edit_role=#{member.id}"
                                                   class="btn btn-sm btn-link text-primary p-1"
                                                   title="Edit role" aria-label="Edit role">
                                                    <i class="fa fa-pencil fa-lg"/>
                                                </a>
                                            </span>
                                            <span class="d-inline-block text-center" style="width: 2.25rem;">
                                                <form t-if="member.user_id.id == request.env.uid"
                                                      t-attf-action="/my/workspaces/#{project.id}/leave"
                                                      method="post" class="d-inline"
                                                      onsubmit="return confirm('Leave this workspace? You will lose access.');">
                                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                    <button type="submit" class="btn btn-sm btn-link text-warning p-1"
                                                            title="Leave workspace" aria-label="Leave workspace">
                                                        <i class="fa fa-sign-out fa-lg"/>
                                                    </button>
                                                </form>
                                                <form t-if="can_manage_ws_users and member.user_id.id != request.env.uid"
                                                      t-attf-action="/my/workspaces/#{project.id}/users/#{member.id}/remove"
                                                      method="post" class="d-inline"
                                                      onsubmit="return confirm('Remove this member from the workspace?');">
                                                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                    <input type="hidden" name="redirect_users" value="1"/>
                                                    <button type="submit" class="btn btn-sm btn-link text-danger p-1"
                                                            title="Remove member" aria-label="Remove member">
                                                        <i class="fa fa-user-times fa-lg"/>
                                                    </button>
                                                </form>
                                            </span>
                                        </div>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <t t-call="itlingo_website_portal.portal_list_pager"/>
        </t>
    </template>

    <template id="portal_workspace_documentation" name="Workspace documentation">
        <t t-call="itlingo_website_portal.portal_workspace_hub_shell">
            <div t-if="doc_message == 'doc_deleted'" class="alert alert-success" role="alert">
                The document was deleted.
            </div>
            <div t-if="doc_error == 'delete_failed'" class="alert alert-danger" role="alert">
                Could not delete this document.
            </div>
            <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3" t-if="can_upload_ws_documents">
                <a t-attf-href="#{ws_hub_prefix}/documents/new" class="btn btn-primary"><i class="fa fa-plus me-1"/>New document</a>
            </div>
            <t t-call="itlingo_website_portal.portal_documents_list">
                <t t-set="doc_link_base" t-value="ws_hub_prefix + '/documents'"/>
            </t>
        </t>
    </template>

    <template id="portal_workspace_documentation_new" name="Workspace documentation new">
        <t t-call="itlingo_website_portal.portal_workspace_hub_shell">
            <div class="card border-0 shadow-sm rounded-3">
                <div class="card-body">
                    <div t-if="error.get('_form')" class="alert alert-danger" role="alert"><t t-out="error['_form']"/></div>
                    <form t-attf-action="/my/workspaces/#{project.id}/documents/new"
                          method="post" enctype="multipart/form-data">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <div class="mb-3">
                            <label class="form-label" for="ws_doc_name">Name</label>
                            <input type="text" class="form-control" id="ws_doc_name" name="name" required=""
                                   t-att-value="form.get('name') or ''"/>
                            <div t-if="error.get('name')" class="text-danger small mt-1"><t t-out="error['name']"/></div>
                        </div>
                        <div class="mb-3">
                            <label class="form-label" for="ws_doc_type">Document type</label>
                            <select class="form-select" id="ws_doc_type" name="document_type_id">
                                <option value="">—</option>
                                <t t-foreach="doc_types" t-as="dt">
                                    <option t-att-value="dt.id"
                                            t-att-selected="str(form.get('document_type_id') or '') == str(dt.id)">
                                        <t t-out="dt.name"/>
                                    </option>
                                </t>
                            </select>
                            <div t-if="error.get('document_type_id')" class="text-danger small mt-1"><t t-out="error['document_type_id']"/></div>
                        </div>
                        <t t-call="itlingo_documents.portal_document_meta_fields"/>
                        <div class="mb-3">
                            <label class="form-label" for="ws_doc_file">File</label>
                            <input type="file" class="form-control" id="ws_doc_file" name="document_file"/>
                        </div>
                        <t t-call="itlingo_documents.portal_document_template_fields"/>
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">Create</button>
                            <a t-attf-href="/my/workspaces/#{project.id}/documents" class="btn btn-outline-secondary">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </t>
    </template>

    <template id="portal_workspace_specifications" name="Workspace specifications">
        <t t-call="itlingo_website_portal.portal_workspace_hub_shell">
            <div class="card border-0 shadow-sm rounded-3">
                <div class="card-body">
                    <div class="d-flex align-items-center mb-3">
                        <div class="rounded-circle bg-primary bg-opacity-10 d-flex align-items-center justify-content-center me-3"
                             style="width:48px;height:48px;">
                            <i class="fa fa-code fa-lg text-primary"/>
                        </div>
                        <div>
                            <h2 class="h5 mb-0">Specifications</h2>
                            <p class="text-muted mb-0 small">
                                Edit ASL and RSL specification files for this workspace using the ITOI online IDE.
                            </p>
                        </div>
                    </div>

                    <div class="border rounded-3 p-4 bg-light">
                        <div class="row align-items-center">
                            <div class="col">
                                <h3 class="h6 mb-1">ITOI — ITLingo Online IDE</h3>
                                <p class="text-muted small mb-0">
                                    Opens in a new tab. Your session is linked to the
                                    <strong><t t-out="project.name"/></strong> workspace.
                                    <t t-if="has_write">You have <span class="badge bg-success">write</span> access.</t>
                                    <t t-else="">You have <span class="badge bg-secondary">read-only</span> access.</t>
                                </p>
                            </div>
                            <div class="col-auto">
                                <a t-att-href="itoi_launch_path"
                                   target="_blank" rel="noopener noreferrer"
                                   class="btn btn-primary">
                                    <i class="fa fa-external-link me-1"/>Launch ITOI
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <!-- Create / edit workspace wizard (4 steps) -->
    <template id="portal_workspace_wizard" name="Workspace wizard">
        <t t-if="wizard_mode == 'edit'">
            <t t-call="itlingo_website_portal.portal_workspace_hub_shell">
                <div>
                    <div class="mb-3">
                        <span class="badge bg-light text-dark">Step <t t-out="wizard_step"/> / 4</span>
                    </div>
                    <t t-call="itlingo_website_portal.portal_workspace_wizard_steps"/>
                </div>
            </t>
        </t>
        <t t-else="">
            <t t-call="portal.portal_layout">
                <div class="container py-3">
                    <h1 class="h4 mb-3">Create workspace</h1>
                    <div class="mb-3">
                        <span class="badge bg-primary">Step <t t-out="wizard_step"/> / 4</span>
                    </div>
                    <t t-call="itlingo_website_portal.portal_workspace_wizard_steps"/>
                </div>
            </t>
        </t>
    </template>

    <template id="portal_workspace_wizard_steps" name="Workspace wizard steps inner">
        <t t-if="wizard_step == 1">
            <div t-if="error.get('name')" class="alert alert-danger" role="alert"><t t-out="error['name']"/></div>
            <div t-if="error.get('organization_id')" class="alert alert-danger" role="alert"><t t-out="error['organization_id']"/></div>
            <div t-if="not organizations" class="alert alert-warning" role="alert">
                You need to be an organization manager to create a workspace.
            </div>
            <form t-if="wizard_mode == 'edit'" t-attf-action="/my/workspaces/#{project.id}/settings/1"
                  method="post" class="card border-0 shadow-sm rounded-3">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label">Organization</label>
                        <p class="form-control-plaintext"><t t-out="project.organization_id.name or '—'"/></p>
                        <label class="form-label" for="ws_wiz_name_e">Workspace name</label>
                        <input type="text" class="form-control" id="ws_wiz_name_e" name="name" required=""
                               t-att-value="form.get('name') or project.name"/>
                    </div>
                    <div class="form-check mb-3">
                        <input class="form-check-input" type="checkbox" name="is_public_workspace" id="ws_wiz_pub_e" value="1"
                               t-att-checked="form.get('is_public_workspace')"/>
                        <label class="form-check-label" for="ws_wiz_pub_e">
                            Public workspace (all users can view documents, specifications and AI chats)
                        </label>
                    </div>
                    <button type="submit" class="btn btn-primary">Continue</button>
                    <a t-attf-href="/my/workspaces/#{project.id}" class="btn btn-outline-secondary">Cancel</a>
                </div>
            </form>
            <form t-if="wizard_mode != 'edit' and organizations" action="/my/workspaces/new/step/1" method="post" class="card border-0 shadow-sm rounded-3">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label" for="ws_wiz_name">Workspace name</label>
                        <input type="text" class="form-control" id="ws_wiz_name" name="name" required=""
                               t-att-value="form.get('name') or ''"/>
                    </div>
                    <div class="mb-3">
                        <label class="form-label" for="ws_wiz_org">Organization</label>
                        <select class="form-select" id="ws_wiz_org" name="organization_id" required="">
                            <t t-foreach="organizations" t-as="org">
                                <option t-att-value="org.id" t-att-selected="org.id == default_org_id">
                                    <t t-out="org.name"/>
                                </option>
                            </t>
                        </select>
                    </div>
                    <div class="form-check mb-3">
                        <input class="form-check-input" type="checkbox" name="is_public_workspace" id="ws_wiz_pub" value="1"
                               t-att-checked="form.get('is_public_workspace')"/>
                        <label class="form-check-label" for="ws_wiz_pub">
                            Public workspace (all users can view documents, specifications and AI chats)
                        </label>
                    </div>
                    <button type="submit" class="btn btn-primary">Continue</button>
                    <a href="/my/workspaces" class="btn btn-outline-secondary">Cancel</a>
                </div>
            </form>
        </t>
        <t t-if="wizard_step == 2">
            <form t-if="wizard_mode == 'edit'" t-attf-action="/my/workspaces/#{project.id}/settings/2"
                  method="post" class="card border-0 shadow-sm rounded-3">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                <div class="card-body row g-3">
                    <div class="col-12">
                        <h2 class="h6 mb-0">Planning <span class="text-muted fw-normal">(optional)</span></h2>
                        <p class="text-muted small mb-0">All dates are optional and can be left empty.</p>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="pstart">Planned start</label>
                        <input type="date" class="form-control" id="pstart" name="planned_start" t-att-value="form.get('planned_start') or ''"/>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="pend">Planned end</label>
                        <input type="date" class="form-control" id="pend" name="planned_end" t-att-value="form.get('planned_end') or ''"/>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="astart">Actual start</label>
                        <input type="date" class="form-control" id="astart" name="actual_start" t-att-value="form.get('actual_start') or ''"/>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="aend">Actual end</label>
                        <input type="date" class="form-control" id="aend" name="actual_end" t-att-value="form.get('actual_end') or ''"/>
                    </div>
                    <div class="col-12 d-flex gap-2">
                        <button type="submit" class="btn btn-primary">Continue</button>
                        <a t-attf-href="/my/workspaces/#{project.id}" class="btn btn-outline-secondary">Cancel</a>
                    </div>
                </div>
            </form>
            <form t-else="" action="/my/workspaces/new/step/2" method="post" class="card border-0 shadow-sm rounded-3">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                <div class="card-body row g-3">
                    <div class="col-12">
                        <h2 class="h6 mb-0">Planning <span class="text-muted fw-normal">(optional)</span></h2>
                        <p class="text-muted small mb-0">All dates are optional and can be left empty.</p>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="pstart_c">Planned start</label>
                        <input type="date" class="form-control" id="pstart_c" name="planned_start" t-att-value="form.get('planned_start') or ''"/>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="pend_c">Planned end</label>
                        <input type="date" class="form-control" id="pend_c" name="planned_end" t-att-value="form.get('planned_end') or ''"/>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="astart_c">Actual start</label>
                        <input type="date" class="form-control" id="astart_c" name="actual_start" t-att-value="form.get('actual_start') or ''"/>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label" for="aend_c">Actual end</label>
                        <input type="date" class="form-control" id="aend_c" name="actual_end" t-att-value="form.get('actual_end') or ''"/>
                    </div>
                    <div class="col-12 d-flex gap-2">
                        <button type="submit" class="btn btn-primary">Continue</button>
                        <a href="/my/workspaces" class="btn btn-outline-secondary">Cancel</a>
                    </div>
                </div>
            </form>
        </t>
        <t t-if="wizard_step == 3">
            <form t-if="wizard_mode == 'edit'" t-attf-action="/my/workspaces/#{project.id}/settings/3"
                  method="post" class="card border-0 shadow-sm rounded-3">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                <div class="card-body">
                    <label class="form-label" for="ws_desc">Description</label>
                    <textarea class="form-control" id="ws_desc" name="description" rows="6"><t t-out="form.get('description') or ''"/></textarea>
                    <div class="mt-3 d-flex gap-2">
                        <button type="submit" class="btn btn-primary">Continue</button>
                        <a t-attf-href="/my/workspaces/#{project.id}" class="btn btn-outline-secondary">Cancel</a>
                    </div>
                </div>
            </form>
            <form t-else="" action="/my/workspaces/new/step/3" method="post" class="card border-0 shadow-sm rounded-3">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                <div class="card-body">
                    <label class="form-label" for="ws_desc_c">Description</label>
                    <textarea class="form-control" id="ws_desc_c" name="description" rows="6"><t t-out="form.get('description') or ''"/></textarea>
                    <div class="mt-3 d-flex gap-2">
                        <button type="submit" class="btn btn-primary">Continue</button>
                        <a href="/my/workspaces" class="btn btn-outline-secondary">Cancel</a>
                    </div>
                </div>
            </form>
        </t>
        <t t-if="wizard_step == 4">
            <div class="card border-0 shadow-sm rounded-3" t-if="wizard_data">
                <div class="card-body">
                    <dl class="row small mb-0">
                        <dt class="col-sm-4">Name</dt><dd class="col-sm-8"><t t-out="wizard_data.get('name')"/></dd>
                        <dt class="col-sm-4">Organization</dt><dd class="col-sm-8"><t t-out="wizard_data.get('organization') or wizard_data.get('organization_id')"/></dd>
                        <dt class="col-sm-4">Public workspace</dt><dd class="col-sm-8"><t t-out="'Yes' if wizard_data.get('is_public_workspace') else 'No'"/></dd>
                        <dt class="col-sm-4">Planned start</dt><dd class="col-sm-8"><t t-out="wizard_data.get('planned_start') or '—'"/></dd>
                        <dt class="col-sm-4">Planned end</dt><dd class="col-sm-8"><t t-out="wizard_data.get('planned_end') or '—'"/></dd>
                        <dt class="col-sm-4">Actual start</dt><dd class="col-sm-8"><t t-out="wizard_data.get('actual_start') or '—'"/></dd>
                        <dt class="col-sm-4">Actual end</dt><dd class="col-sm-8"><t t-out="wizard_data.get('actual_end') or '—'"/></dd>
                    </dl>
                    <form t-if="wizard_mode != 'edit'" action="/my/workspaces/new/step/4" method="post" class="mt-3 d-flex gap-2">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <button type="submit" class="btn btn-primary">Create workspace</button>
                        <a href="/my/workspaces" class="btn btn-outline-secondary">Cancel</a>
                    </form>
                    <a t-attf-href="/my/workspaces/#{project.id}" t-if="wizard_mode == 'edit'" class="btn btn-primary">Done</a>
                </div>
            </div>
        </t>
    </template>

    <template id="portal_workspace_leave_promote" name="Workspace leave promote">
        <t t-call="itlingo_website_portal.portal_workspace_hub_shell">
            <div class="card border-0 shadow-sm rounded-3">
                <div class="card-header bg-white">
                    <h5 class="mb-0">Promote a successor before leaving</h5>
                </div>
                <div class="card-body">
                    <p class="text-muted">
                        You are the last Workspace Manager of this workspace.
                        Pick another member to promote to Workspace Manager before
                        you leave.
                    </p>
                    <div t-if="error" class="alert alert-danger" role="alert">
                        <t t-out="error"/>
                    </div>
                    <form t-attf-action="/my/workspaces/#{project.id}/leave/promote"
                          method="post">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <div class="list-group mb-3">
                            <label t-foreach="candidates" t-as="cand"
                                   class="list-group-item d-flex align-items-center gap-2">
                                <input type="radio" class="form-check-input m-0"
                                       name="successor_role_id" t-att-value="cand.id" required=""/>
                                <span class="ms-2"><t t-out="cand.user_id.name"/></span>
                                <span class="badge bg-info text-dark ms-auto">
                                    <t t-out="dict(cand._fields['role'].selection).get(cand.role)"/>
                                </span>
                            </label>
                        </div>
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-warning"
                                    onclick="return confirm('Promote the selected member and leave the workspace?');">
                                Promote and leave
                            </button>
                            <a t-attf-href="/my/workspaces/#{project.id}/users" class="btn btn-outline-secondary">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </t>
    </template>

</odoo>
